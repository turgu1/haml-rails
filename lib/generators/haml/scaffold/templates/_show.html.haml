.legend_u
  #{@<%= singular_table_name %>.<%= attributes.first.name %>}
  = render partial: 'edit_link', object: @<%= singular_table_name %>

= show_entry @<%= singular_table_name %> do |e|
<% for attribute in attributes -%>
  = e.field :<%= attribute.name %>
<% end -%>

= remote_link_to 'Back', <%= index_helper %>_path
